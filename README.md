# ExpressMongodbApiTemplate
A default template for a Express api with mongodb

# Installation
1 - Change the name of ```.env.example``` to ```.env```

2 - Run this command in the bash
```bash
docker-compose up -d
```
